import 'package:flutter/material.dart';

class AppColors {
  /// Fondo Negro
  static const Color backgroundDark     = Color(0xFF111111);
  /// Fondo Gris
  static const Color backgroundGrey     = Color(0xFF222222);





  /// Color secundario para acciones/enlaces (Categories)
  static const Color accentBlue         = Color(0xFF0085FF);
  /// Texto descriptivo (Categories)
  static const Color neutralGray        = Color(0xFF999999);

  static const Color testimonialArrowBg = Color(0xFF0085FF);
  /// Fondo de cada tarjeta (Categories)
  static const Color categoryCardBg     = Color(0xFF111111);

  /// Borde de cada tarjeta  (Categories)
  static final Color categoryCardBorder = Colors.white.withOpacity(0.1);

  /// Blanco para textos
  static const Color white = Colors.white;

  /// Fondo de la tarjeta (Testimonial)
  static final Color testimonialCardBg  = Colors.white.withOpacity(0.08);

  /// Borde de la tarjeta (Testimonial)
  static final Color testimonialCardBorder = Colors.white.withOpacity(0.1);

}


